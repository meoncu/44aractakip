rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Giris yapmis herhangi bir kullanici kendi verilerini okuyup yazabilir
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
